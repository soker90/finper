import { ModalGrid } from 'components'

const ModalEdit = ({ onClose, show }: any) => {
  return <ModalGrid title='E' onClose={onClose} show={show}>
        hola
    </ModalGrid>
}

export default ModalEdit
